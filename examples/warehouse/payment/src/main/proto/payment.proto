syntax = "proto3";

package choral.accompanist.examples.warehouse.proto;

service Payment {
  rpc CommitTakeMoneyFromCustomer (Empty) returns (Empty) {}
  rpc CompensateTakeMoneyFromCustomer (Empty) returns (Empty) {}
}

message Empty {}
