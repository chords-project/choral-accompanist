# Shared runtime defaults for the Accompanist/Temporal recovery benchmark.
# Both Kubernetes profiles load this file through the benchmark-runtime-settings ConfigMap.
EXECUTION_CONCURRENCY=32
DELIVERY_CONCURRENCY=32
SCAN_BATCH_SIZE=512
WORKER_POLLERS=16
DB_POOL_SIZE=32
