package org.choral.accompanist.channels;

import org.choral.accompanist.channels.SymChannel;

public interface AsyncSymChannel@(A, B)<T@X> extends
    AsyncDiChannel@(A, B)<T>,
    AsyncDiChannel@(B, A)<T>,
    SymChannel@(A,B)<T> {}
